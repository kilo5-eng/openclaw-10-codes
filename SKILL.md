---\nname: volume-spread-analysis\ndescription: Performs full Volume Spread Analysis (VSA) on any stock, ETF, or crypto symbol. Fetches OHLCV data, computes spread/relative volume/close-location, classifies bars (strength/weakness/no-demand/upthrust/etc.), detects climactic volume, and returns actionable signals with context.\nuser-invocable: true\nmetadata:\n  openclaw:\n    requires:\n      packages: [\"yfinance\", \"pandas\", \"numpy\"]\n      bins: [\"python\"]\n---\n\n# Volume Spread Analysis (VSA) Skill\n\n## When to activate\n- User asks about VSA, \"analyze [TICKER]\", \"VSA on AAPL\", \"volume spread on TSLA\", \"what does the volume say about NVDA\", or any daily/weekly scan request.\n- Background heartbeat scans (if you configure a scheduler skill).\n- Before any trade decision (cross-reference with your SOUL.md risk rules).\n\n## Workflow (follow exactly)\n1. Parse the requested symbol(s) and timeframe (default: 1D, last 60 bars; support 15m/1h/4h/D/W).\n2. Use the `run_python_script` tool (or shell `python`) to execute `scripts/vsa_analyzer.py` with the symbol and timeframe.\n3. The script returns a clean JSON with per-bar and overall signals.\n4. Interpret the JSON in natural language:\n   - Highlight high-conviction setups (e.g., \"Stopping volume + close near high after downtrend = potential demand\").\n   - Flag risks (narrow spread + high volume = no demand / trap).\n   - Compare to previous bars for \"effort vs result\".\n   - Suggest next actions (watch for confirmation bar, potential entry, stop placement).\n5. If user wants alerts, append to a watchlist or send via your messaging channel.\n6. Always include relative volume vs 20-bar average and close-location %.\n\n## Available tools you will use\n- `run_python_script` (or equivalent shell execution) pointing to `scripts/vsa_analyzer.py`\n- Any existing data-fetch tools (yfinance is pre-installed via metadata)\n- Your messaging tools for alerts\n\n## VSA principles (reference only – see references/vsa_rules.md for full taxonomy)\n- **Spread** = High - Low\n- **Close location** = ((Close - Low) / Spread) * 100\n- **Relative volume** > 1.5× average = significant effort\n- **Strength signals**: High vol + wide spread + close near high (especially after weakness)\n- **Weakness signals**: High vol + narrow spread + close near low (upthrust, no demand)\n- Context is everything: compare to background trend and prior bars.\n\nKeep output concise, professional, and trade-ready. Always remind: \"This is analysis only – confirm with multiple timeframes and your full system.\"