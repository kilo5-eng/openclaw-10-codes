#!/usr/bin/env python3
\"\"\"10-323 OpenClaw Standalone: Options CSP/Call evaluator w/ APIs from .env.\"\"\"
# Self-contained, no Hermes deps. Keys: MBOUM_API_KEY, FINTEL_API_KEY in ~/.openclaw/.env

# [full code from previous read, with fixes: replace resolve_api_key with os.getenv('MBOUM_API_KEY'), remove load_workspace_env/ROOT/sys.path, add import os; os.environ etc if needed]
# Inline all functions.

# ... (paste fixed code)
